# CLAUDE.md — 开发行为强约束规范
# 本文件对 Claude Code 具有最高优先级，所有开发行为必须遵守以下规则。
# 违反任何一条规则，视为无效交付。

---

## ⛔ 绝对禁止的行为

1. **禁止一次性生成所有代码**
   - 不得在单次对话或单次操作中完成整个项目的全部代码
   - 每次只完成一个明确的、独立的功能单元

2. **禁止批量创建文件后统一提交**
   - 不得先生成10个文件，再一次性 `git add . && git commit`
   - 每个逻辑模块完成后必须立即单独提交

3. **禁止空白或敷衍的 commit message**
   - 不接受：`fix`, `update`, `done`, `wip`, `test`
   - 必须描述：做了什么、为什么这样做

4. **禁止空白 PR 描述**
   - 每个 PR 必须包含：变更目的、实现方式、涉及文件

---

## ✅ 必须遵守的开发流程

### 每次开始工作前

```
1. 确认当前处于正确的 feature branch（不在 main 上直接开发）
2. 明确本次只完成哪一个功能点
3. 告知我：本次计划做什么、预计产生哪些文件变更
```

### 每完成一个功能单元后，必须立即执行

```bash
# 1. 只 add 本次相关的文件（禁止 git add .）
git add <具体文件>

# 2. 使用规范的 commit message
git commit -m "<type>(<scope>): <具体描述>

- 做了什么（what）
- 为什么这样做（why）
- 如何实现的关键点（how，可选）"

# type 必须从以下选择：
# feat     新功能
# fix      修复 bug
# refactor 重构（不改变功能）
# docs     文档变更
# test     测试相关
# chore    构建/配置变更
```

### PR 创建规范

每个 PR 必须包含以下内容（缺一不可）：

```markdown
## 本次变更目的
[清晰说明这个 PR 解决了什么问题或实现了什么功能]

## 实现方式
[简述技术实现路径，关键决策说明]

## 涉及文件
- `path/to/file.py`：[这个文件做了什么变更]
- `path/to/another.py`：[这个文件做了什么变更]

## 第三方依赖（如有新增）
- 库名 vX.X：用途说明，为何选择此库

## 代码来源说明（如有复用）
- 如复用了过去的代码片段，在此注明来源和复用原因

## 测试验证
[说明如何验证这次变更是正确的]
```

---

## 📅 开发节奏规范

### 功能拆解原则
拿到任何开发任务，必须先拆解为至少以下粒度的阶段：

```
阶段1：项目初始化 + 基础结构
阶段2：核心数据模型 / 配置层
阶段3：核心功能模块 A
阶段4：核心功能模块 B
阶段5：核心功能模块 C
阶段6：模块间集成
阶段7：界面 / 接口层
阶段8：错误处理 + 边界情况
阶段9：文档完善 + README
阶段10：最终测试 + 修复
```

每个阶段 = 至少1个 PR + 若干 commit，不得合并阶段。

### commit 频率要求
- 每完成一个独立函数/类/模块 → 1个 commit
- 每修复一个 bug → 1个 commit
- 每次调整配置或依赖 → 1个 commit
- **单次会话内 commit 数量下限：3个**

---

## 📋 README 依赖声明规范

每次新增第三方库后，必须立即更新 README.md 的依赖章节：

```markdown
## 依赖说明

| 库名 | 版本 | 用途 | 原创功能说明 |
|------|------|------|------------|
| streamlit | >=1.32 | Web UI 框架 | 自定义三Tab布局、状态轮询逻辑为原创实现 |
| playwright | >=1.44 | 浏览器自动化 | 各平台发布器的操作逻辑、重试策略为原创 |
| openai | >=1.30 | AI API 调用 | Prompt设计、多平台适配逻辑为原创 |
```

---

## 🔍 每次提交前的自查清单

在执行任何 `git commit` 前，必须确认：

- [ ] 本次 commit 只包含一个逻辑单元的变更
- [ ] commit message 描述了 what 和 why
- [ ] 没有使用 `git add .`（除非所有文件都属于同一逻辑单元）
- [ ] 新增了第三方库 → README 已更新
- [ ] 复用了旧代码 → PR 描述中已注明来源
- [ ] 代码中没有 TODO/FIXME 遗留在本次提交范围内（除非 commit message 明确说明）

---

## 🚨 违规检测

以下情况出现时，我会要求你回滚并重新操作：

1. `git log` 显示连续多个 commit 时间戳相同或间隔小于30秒
2. 单个 commit 的 diff 超过300行（说明粒度太粗）
3. PR 的 Files Changed 与 PR 描述完全对不上
4. `git add .` 出现在 commit 前（需说明理由）

---

## 📌 本项目特定约束

> 以下是针对当前项目（AI多平台发布工具）的具体约束

**开发批次时间窗口**：[在此填写你的批次开始和截止时间]

**强制分支策略**：
```
main                    # 只接受 PR 合并，不直接 push
├── feat/config         # 配置层
├── feat/task-manager   # 任务状态机
├── feat/ai-processor   # AI处理模块
├── feat/publisher-base # 发布器基类
├── feat/publisher-xhs  # 小红书发布器
├── feat/publisher-zhihu# 知乎发布器
├── feat/publisher-tieba# 贴吧发布器
├── feat/publisher-wechat# 公众号发布器
├── feat/app-ui         # Streamlit界面
└── docs/readme         # 文档
```

**每个分支对应一个 PR，PR 合并后分支删除。**

---

## 执行说明

当我说"开始开发"时，你必须：
1. 首先输出本次开发计划（哪个阶段、哪个分支、预计哪些 commit）
2. 等我确认后再开始写代码
3. 每完成一个 commit 单元，停下来告知我，等我确认后继续

**不得在未经确认的情况下连续完成多个阶段。**
